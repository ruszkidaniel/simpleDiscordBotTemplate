import { Invite } from "discord.js";
import { DiscordBot } from "../bot";

export async function inviteCreate(inv: Invite) {
    //const invites = await DiscordBot.fetchInvites();
    //DiscordBot.storeInvites(invites);
}